※ この課題では全てのフィールド・引数・戻り値に型宣言を付けること

## 問題 1

Userクラスに住所(string)を管理するフィールドを作成し、main.phpのshowUserStatus内で表示するようにしなさい。

追加するもの:
- `private string $address` フィールド
- コンストラクタに `string $address` 引数を追加
- `setAddress(string $address): void`
- `getAddress(): string`

## 問題 2

UserクラスのgetOlder関数を以下の仕様通りに完成させなさい。
引数: なし
戻り値: なし(void)
実行するとageが現在値より1増える。ただし99より大きくはならない。
